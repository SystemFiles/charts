{{ range .Values.secret.repositories }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .name }}
  namespace: argocd-system
  labels:
    argocd.argoproj.io/secret-type: repository
stringData:
  type: git
  url: {{ .gitUrl }}
  password: {{ .gitPassword }}
  username: {{ .gitUsername }}
{{ end }}
