apiVersion: v2
name: bootstrap
description: A helm chart for bootstrapping an RKE2 cluster platform
type: application

version: 0.1.1
appVersion: "1.16.0"

dependencies:
  - name: core
    condition: core.enabled
    version: 0.1.1
  - name: application
    condition: application.enabled
    version: 0.1.0

